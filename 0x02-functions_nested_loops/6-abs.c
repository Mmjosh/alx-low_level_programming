#include "main.h"
/**
 * _abs - computes the absolute value of a digit
 * @t: The integer to be computed
 * Return: the absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
