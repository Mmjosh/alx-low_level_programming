#include <stdio.h>
/**
 * main - prints the program name
 * @argc: argument count
 * @argv: pointer to an array of pointers
 * Return: 0
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);

	return (0);
}
